name: Update README

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:
    inputs:
      download_videos:
        description: 'Video download mode: false (skip), auto/true require GH_SESSION_TOKEN'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'auto'
          - 'true'
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - '.github/workflows/update-readme.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Install video tools
        if: ${{ (inputs.download_videos || 'false') != 'false' }}
        env:
          GH_SESSION_TOKEN: ${{ secrets.GH_SESSION_TOKEN }}
        run: |
          if [ -z "${GH_SESSION_TOKEN:-}" ]; then
            echo "::error::GH_SESSION_TOKEN is required when download_videos is auto or true"
            exit 1
          fi
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          pip install yt-dlp
          gh extension install drogers0/gh-image

      - name: Cache video downloads
        if: ${{ (inputs.download_videos || 'false') != 'false' }}
        uses: actions/cache@v4
        with:
          path: tmp/videos
          key: videos-${{ hashFiles('video-urls.json') }}
          restore-keys: videos-

      - run: pnpm run generate
        env:
          CMS_HOST: ${{ secrets.CMS_HOST }}
          CMS_API_KEY: ${{ secrets.CMS_API_KEY }}
          DOWNLOAD_VIDEOS: ${{ inputs.download_videos || 'false' }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_SESSION_TOKEN: ${{ secrets.GH_SESSION_TOKEN }}

      - name: Commit and push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md README_*.md video-urls.json
          git diff --quiet && git diff --staged --quiet || git commit -m "docs: auto-update README [skip ci]"
          git push
